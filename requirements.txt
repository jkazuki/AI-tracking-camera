# requirements.txt
# Core Vision & AI
ultralytics>=8.0.0
opencv-python>=4.8.0
numpy>=1.24.0

# Geometry
shapely>=2.0.1

# Screen Capture (Cross-platform & Windows specific)
mss>=7.0.1
pywin32>=306 ; sys_platform == 'win32'
windows-capture>=1.0.2 ; sys_platform == 'win32'

# Face Re-ID (Optional but highly recommended)
face_recognition>=1.3.0
